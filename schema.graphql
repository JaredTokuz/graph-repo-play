enum TradeType {
  Mint
  Burn
}

type TokenTrade @entity(immutable: true) {
  id: Bytes!
  nonce: BigInt!
  er20Amount: BigInt!
  ethAmount: BigInt!
  timestamp: BigInt!
  tradeType: TradeType!
  address: String!
}

type AddressProfile @entity {
  id: Bytes!
  address: String!
  erc20Purchased: BigInt!
  erc20Sold: BigInt!
  weiSpent: BigInt!
  weiSold: Bigint!
  weiNetRealized: BigInt!
  noTrades: BigInt!
}

type LatestTokenState @entity {
  id: String!
  last_timestamp: BigInt!
  last_nonce: BigInt!
  price: BigInt!
  weiSpent: BigInt!
  weiWithdrawn: BigInt!
  noAddress: BigInt!
  noTrades: BigInt!
}
